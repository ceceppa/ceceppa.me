.wp-block-embed {
  margin: 0;

  figcaption {
    font-size: 13px;
    text-align: center;
    color: var(--color-black);
  }

  .wp-block-embed__wrapper {
    position: relative;
    padding-bottom: 56%;

    iframe {
      position: absolute;
      width: 100%;
      height: 100%;
    }
  }
}
